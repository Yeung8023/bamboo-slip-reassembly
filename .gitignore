# Regenerated deterministically from seeds by `src/matcher.py pairs`, and far
# over GitHub's file size limit.
data/train_pairs.npz

# Manuscript, submission package and internal working notes are not part of the
# code and data release.
paper/
submission/
figs_backup_jas/
paper.txt
HANDOFF.md

__pycache__/
*.pyc

# DeepJiandu photographs: 3 GB, redistributed by Science Data Bank under its
# own DOI, and rebuilt from there by revision/code/exp_real.py. Only the list
# of files that passed segmentation is kept, so the corpus is reproducible.
revision/data_real/*.zip
revision/data_real/images/
revision/logs/
